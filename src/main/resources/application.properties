spring.application.name=bancodigital

# --- CONFIG ANTIGA (POSTGRES) COMENTADA ---
# spring.datasource.url=jdbc:postgresql://localhost:5432/banco_digital
# spring.datasource.username=postgres
# spring.datasource.password=1234

# --- CONFIG ATUAL (H2 PARA AWS) ---
# Banco H2 que salva em arquivo no servidor
spring.datasource.url=jdbc:h2:file:./dados_banco
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Hibernate e Console
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true